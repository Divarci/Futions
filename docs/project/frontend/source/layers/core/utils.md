# Core — Utils

Pure utility functions with no side effects, no API calls, and no React dependencies.

---

## Rules

- No side effects.
- No API calls.
- No React dependencies.
- Group by concern — one file per concern (e.g., `date.utils.ts`, `string.utils.ts`).

---

## Examples

`date.utils.ts` — date formatting and parsing helpers.
